{
  "name": "@bennett/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "@bennett/shared": "file:../shared",
    "firebase-admin": "^13.6.0",
    "stripe": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vercel": "^48.0.0"
  }
}
